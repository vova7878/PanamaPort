pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "PanamaPort"
include ':keep_annotation'
include ':fake_panama'
include ':fake_buffer'
include ':AndroidUnsafe'
include ':AndroidPanama'
include ':VarHandleApi'
